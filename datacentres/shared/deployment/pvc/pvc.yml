apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvcXXX
  namespace: namespaceXXX
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2Ti
  storageClassName: ceph-cephfs
  volumeMode: Filesystem
